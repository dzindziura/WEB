<!-- Модальне вікно -->
<div class="modal fade" id="modal_ports" data-bs-backdrop="static" data-bs-keyboard="true" tabindex="-1"
  aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content bg-dark">

      <!-- Заголовок модального вікна -->
      <div class="modal-header border-secondary">
        <div class="d-flex flex-column">
          <span class="fs-3 lh-1" id="port_title">#Title</span>
        </div>
        <button type="button" class="btn-close bg-primary" data-bs-dismiss="modal"></button>
      </div>

      <!-- Тіло модального вікна -->
      <div class="modal-body">
        <form>

          <!-- Назва Порту -->
          <span class="fs-5">Назва Порту</span>
          <input class="form-control fs-5 bg-dark text-light border-primary mb-2" id="port_name" type="text"
            placeholder="Назва">

          <!-- Адреса Порту -->
          <span class="fs-5">Країна Порту</span>
          <input class="form-control fs-5 bg-dark text-light border-primary" id="port_country" type="text"
            placeholder="Країна">
          <span class="fs-5">Адреса Порту</span>
          <input class="form-control fs-5 bg-dark text-light border-primary" id="port_address" type="text"
            placeholder="Адреса">
        </form>
      </div>

      <!-- Кнопки модального вікна -->
      <div class="modal-footer border-secondary">
        <button class="btn btn-outline-primary" data-bs-dismiss="modal" onclick="modal_add_ports()"
          id="port_yes">#Yes</button>
        <button class="btn btn-outline-secondary" data-bs-dismiss="modal" id="port_no">Відміна</button>
      </div>
    </div>
  </div>
</div>